algoritmo "Print salteando linha junto com variáveis"

var idade: inteiro //*variaveis também podem ser palavras e não apenas números :P
//concatenar / concatenação: juntar uma sequência lógica / ex.: escreva(texto+var) (lembrar dessa palavra)

inicio

    idade <- 17

    escreva("Minha idade: ", idade)
   
fimalgoritmo