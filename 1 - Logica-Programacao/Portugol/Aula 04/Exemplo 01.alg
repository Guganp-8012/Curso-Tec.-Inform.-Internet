algoritmo "Comparações"

var a, b: inteiro //é possivel simplesmente separar as variáveis por "," ao invés de pular uma linha

inicio

    a <- 5
    b <- 2

    escreval(a > b) //verdadeiro
    escreval(a = b) //falso
    escreva(a < b) //falso

fimalgoritmo